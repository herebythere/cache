version: "3"
services:
  cache:
    container_name: cache_redis_${container_port}
    build:
      context: ./
      dockerfile: redis.podmanfile
    ports:
      - ${host_port}:6379
    volumes:
      - ./data/:/data:z
